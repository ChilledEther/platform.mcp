---
phase: 01-core-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [go.mod, pkg/scaffold/types.go, pkg/scaffold/types_test.go, pkg/scaffold/generator.go]
autonomous: true
---

<objective>
Initialize the module structure and implement the core `Config` and `File` types and the base generation interface using TDD.

Purpose: Establish the foundation for the library and set TDD patterns.
Output: Valid go.mod, core types with validation logic, and Generator interface.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Project Skeleton & Module Init</name>
  <files>go.mod</files>
  <action>
    Initialize the Go module as `github.com/helper-j/platform.mcp`.
    Create the necessary directory structure: `pkg/scaffold` and `internal/templates`.
  </action>
  <verify>
    `go mod verify` passes and directories exist.
  </verify>
  <done>
    Project skeleton initialized.
  </done>
</task>

<task type="auto">
  <name>Task 2: Define Types (TDD)</name>
  <files>pkg/scaffold/types.go, pkg/scaffold/types_test.go</files>
  <action>
    Define `Config` and `File` structs in `pkg/scaffold/types.go`.
    Implement validation logic for `Config` (e.g., ensuring `ProjectName` is not empty).
    Write tests in `pkg/scaffold/types_test.go` following the RED-GREEN-REFACTOR cycle.
  </action>
  <verify>
    `go test ./pkg/scaffold` passes.
  </verify>
  <done>
    Core types defined and validated via TDD.
  </done>
</task>

<task type="auto">
  <name>Task 3: Base Generator Interface (TDD)</name>
  <files>pkg/scaffold/generator.go</files>
  <action>
    Define the `Generator` interface in `pkg/scaffold/generator.go`.
    Implement a mock or stub generator to verify the interface's behavior in tests.
  </action>
  <verify>
    Interface is correctly implemented and return types match expectations.
  </verify>
  <done>
    Generation interface defined.
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `go mod verify` succeeds.
- [ ] `go test ./pkg/scaffold` passes.
</verification>

<success_criteria>
- [ ] go.mod created and valid.
- [ ] pkg/scaffold exists.
- [ ] Config struct validation covered by tests.
- [ ] Core types and interface exported.
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-foundation/01-01-SUMMARY.md`
</output>
