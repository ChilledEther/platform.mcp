# Plan: Implement Core Logic (TDD)

## Objective
Initialize the module structure and implement the core `Config` and `File` types and the base generation interface using TDD.

## Context
- First code in the project.
- Must establish TDD patterns.
- No external dependencies for this plan.

## Tasks

- [ ] **Task 1: Project Skeleton & Module Init**
  - Initialize `go.mod` (module `github.com/helper-j/platform.mcp`).
  - Create `pkg/scaffold` and `internal/templates` directories.
  - Verification: `go mod verify` passes.

- [ ] **Task 2: Define Types (TDD)**
  - Create `pkg/scaffold/types.go`.
  - Define `Config` and `File` structs.
  - Write test `pkg/scaffold/types_test.go` to verify struct validation logic (e.g., ProjectName required).
  - Implement validation logic to pass tests.

- [ ] **Task 3: Base Generator Interface (TDD)**
  - Define `Generator` interface.
  - Implement a mock/stub generator for testing.
  - Write test to ensure `Generate` returns expected `[]File` structure.

## Success Criteria
- [ ] `go.mod` created and valid.
- [ ] `pkg/scaffold` exists.
- [ ] `Config` struct validation covered by tests.
- [ ] `go test ./pkg/scaffold` passes.
