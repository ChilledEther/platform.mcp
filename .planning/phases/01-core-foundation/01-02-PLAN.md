---
phase: 01-core-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [internal/templates/loader.go, internal/templates/loader_test.go, internal/templates/workflow.yaml.tmpl, internal/templates/docker-build.yaml.tmpl]
autonomous: true
---

<objective>
Implement the template loading mechanism using Go's `embed` package to bundle assets into the binary.

Purpose: Enable the library to carry its own templates as static assets.
Output: Embedded template loader and renderer with variable substitution support.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Template Structure & Dummy Templates</name>
  <files>internal/templates/workflow.yaml.tmpl, internal/templates/docker-build.yaml.tmpl</files>
  <action>
    Create the `internal/templates` directory if it doesn't exist.
    Add sample templates: `workflow.yaml.tmpl` and `docker-build.yaml.tmpl`.
    Add variable placeholders like `{{ .ProjectName }}`.
  </action>
  <verify>
    Template files exist on disk.
  </verify>
  <done>
    Initial templates created.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement Loader (TDD)</name>
  <files>internal/templates/loader.go, internal/templates/loader_test.go</files>
  <action>
    Use `//go:embed` in `internal/templates/loader.go` to bundle the template files.
    Implement `Load(name string) (string, error)` to retrieve template content.
    Verify loading success and failure cases in `loader_test.go`.
  </action>
  <verify>
    `go test ./internal/templates` passes.
  </verify>
  <done>
    Templates are embedded and loadable.
  </done>
</task>

<task type="auto">
  <name>Task 3: Template Rendering (TDD)</name>
  <files>internal/templates/loader.go</files>
  <action>
    Implement `Render(tmpl string, data any) (string, error)` using the standard `text/template` package.
    Ensure it correctly substitutes variables from the `Config` struct.
  </action>
  <verify>
    Tests prove that `{{ .ProjectName }}` is replaced with the actual project name.
  </verify>
  <done>
    Templates can be rendered with dynamic data.
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `go test ./internal/templates` passes all tests.
- [ ] Templates are successfully embedded in the binary.
</verification>

<success_criteria>
- [ ] Templates embedded in binary.
- [ ] Loader can read embedded files.
- [ ] Renderer correctly handles variable substitution.
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-foundation/01-02-SUMMARY.md`
</output>
