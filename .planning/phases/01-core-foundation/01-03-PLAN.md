# Plan: Implement Specific Generators

## Objective
Implement the specific logic for GitHub Actions, Docker, and FluxCD generation, wiring up the types (01-01) and templates (01-02).

## Context
- Depends on Plan 01-01 and 01-02.
- Core business logic of the library.

## Tasks

- [ ] **Task 1: Actions Generator (TDD)**
  - Create `pkg/scaffold/actions.go`.
  - Write test verifying `.github/workflows/ci.yaml` is generated.
  - Implement using `workflow.yaml.tmpl`.
  - Ensure correct file mode (0644).

- [ ] **Task 2: Docker Generator (TDD)**
  - Create `pkg/scaffold/docker.go`.
  - Write test verifying `Dockerfile` and `docker-compose.yaml` (if applicable).
  - Implement using `docker-build.yaml.tmpl`.

- [ ] **Task 3: FluxCD Generator (TDD)**
  - Create `pkg/scaffold/flux.go`.
  - Write test verifying `fluxcd.yaml`.
  - Implement using `fluxcd.yaml.tmpl`.

- [ ] **Task 4: Main Generate Entrypoint**
  - Wire all sub-generators into the main `Generate(cfg Config)` function.
  - Integration test: Call `Generate` and check for presence of all files based on flags.

## Success Criteria
- [ ] `Generate` returns correct `[]File` slice.
- [ ] All file paths are correct (e.g., `.github/workflows/...`).
- [ ] Tests cover all generator types.
- [ ] 100% pure logic, no I/O.
