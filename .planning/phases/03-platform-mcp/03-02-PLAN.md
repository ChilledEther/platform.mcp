---
phase: 03-platform-mcp
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: [internal/mcp/tools.go]
autonomous: true
---

<objective>
Expose core generation logic as MCP tools.

Purpose: Enable AI agents to trigger file generation via the MCP protocol.
Output: MCP server with a `generate` tool.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@pkg/scaffold/types.go
@pkg/scaffold/project.go
@.planning/phases/03-platform-mcp/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate Tool Implementation</name>
  <files>internal/mcp/tools.go</files>
  <action>
    1. Define `generate` tool schema (matching `scaffold.Config`).
    2. Implement tool handler that calls `scaffold.NewProjectGenerator().Generate()`.
    3. Return the generated files as tool results.
  </action>
  <verify>
    MCP inspector or manual testing confirms the `generate` tool is available and returns expected content.
  </verify>
  <done>
    `generate` tool is functional via MCP.
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `generate` tool is registered and functional.
</verification>

<success_criteria>
- [ ] `generate` tool exposed.
- [ ] Tool output correctly returns scaffolded files.
</success_criteria>

<output>
After completion, create `.planning/phases/03-platform-mcp/03-02-SUMMARY.md`
</output>
