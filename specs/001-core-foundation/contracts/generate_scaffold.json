{
  "name": "generate_scaffold",
  "description": "Generates GitHub Actions workflows and project scaffolding based on configuration.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "project_name": {
        "type": "string",
        "description": "The name of the project"
      },
      "use_docker": {
        "type": "boolean",
        "description": "Whether to include Docker-related workflows"
      },
      "workflow_type": {
        "type": "string",
        "enum": ["standard", "docker", "minimal"],
        "description": "The workflow template to use (standard, docker, minimal)"
      }
    },
    "required": ["project_name"]
  }
}
